def square_array(array)
  new_array = []
  
  array.length.times do |something|
    new_array.push(array[something]**2)
  end
  return new_array
end

square_array([1,2,3])

# takes each element in an array
# squares that element
# retrns new array containing the squared elements


#define a method that receives an array and returns a new array with the values squared in place

def squared_array(array)

#Define local array for output
returnarray = []

#loop over each value of the array
while

# for each value square that value and place into local array for aoutpu

# after loop return local array


